# Copiar a .env solamente si todavia no existe.
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=change_me
DB_NAME=merceria
PORT=3000
# true solo para crear/sincronizar una base local de desarrollo.
DB_SYNCHRONIZE=false
